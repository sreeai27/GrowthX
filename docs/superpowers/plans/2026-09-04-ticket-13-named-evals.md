# Ticket 13: Named evaluations

1. Add a strict, deterministic evaluation registry covering policy, transitions, speech, mapping, explanation, Replay, unsafe advice, approval bypass and false-pass cases.
2. Produce versioned run and criterion records with latency and zero fixture cost; fail the run for any critical bypass.
3. Persist tenant-scoped runs and promoted corrections behind an evaluation gateway.
4. Replace the Studio placeholder with a protected evaluation page showing aggregate and case evidence.
5. Add unit and browser tests, then run all required checks and create a Git checkpoint.
